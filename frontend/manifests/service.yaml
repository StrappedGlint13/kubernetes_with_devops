apiVersion: v1
kind: Service
metadata:
  name: todos-frontend
spec:
  type: ClusterIP
  selector:
    app: todos-frontend
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 3000
